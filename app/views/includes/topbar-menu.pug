.topbar__hamburger.hidden-lg.hidden-md(data-action="toggle-sidebar-menu")
  +icon('menu')
  .topbar__hamburger-text Menu
span.hidden-sm.hidden-xs
  if menus && menus.main
    each item in menus.main
      a(href=item.url).topbar__items-link= item.title
